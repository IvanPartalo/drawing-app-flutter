import 'dart:ui';

class DisplayText {
  final String text;
  final Offset position;
  final Color textColor;
  final double fontSize;
  DisplayText(this.text, this.position, this.textColor, this.fontSize);
}